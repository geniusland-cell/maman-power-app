# Netlify configuration for maman-power-app (Vendor App)

[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_ENV = "production"
VITE_APP_ENV = "production"

# Redirects for SPA
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
